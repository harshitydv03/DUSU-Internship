# Copy this file to .env.local and fill in real values — never commit .env.local to git
#
# MongoDB Atlas connection string
# Get your credentials from: https://cloud.mongodb.com → Database Access
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.tjxhapv.mongodb.net/dusu?appName=mongosh+2.9.2

# Auth — change JWT_SECRET to a long random string in production
JWT_SECRET=dusu-dev-secret-change-in-production
ADMIN_USER=admin
ADMIN_PASSWORD=dusu@2026

# Server port (optional, defaults to 5000)
PORT=5000
