# MongoDB connection string
# Local:   mongodb://localhost:27017/dusu
# Atlas:   mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/dusu
MONGODB_URI=mongodb://localhost:27017/dusu

# Auth — always change JWT_SECRET in production
JWT_SECRET=dusu-dev-secret-change-in-production
ADMIN_USER=admin
ADMIN_PASSWORD=dusu@2026

# Server port (optional, defaults to 5000)
PORT=5000
